# 需求澄清说明：无人机巡检照片分图工具工程化升级 PRD

> 文档状态：决策已确认，待开发  
> PRD 版本：1.1  
> 当前软件版本：1.0.1  
> 目标发布版本：1.1.0（D-01 已确认）  
> 适用范围：根目录权威源码，不包含 `repo_tmp/gui_classifier/` 历史副本

> 新增独立需求：KML 转经纬度台账的 Dify 工作流及分图软件接入，见 [PRD_KML经纬度台账_Dify工作流.md](./PRD_KML经纬度台账_Dify工作流.md)。该需求单独评审、开发与验收，不并入本 PRD 既有工程化升级范围。

## 一、用户原始诉求

在保持现有手动飞行、自主飞行、Windows GUI、单 EXE 多模式启动和自动升级能力不被破坏的前提下，继续开发并提升项目的可靠性、可维护性、可验证性和发布质量。

本阶段优先完成以下工作：

1. 建立可追踪、可回滚的源码与构建基线。
2. 为核心分类规则建立脱敏、可重复执行的回归测试。
3. 增加分类前预检、仅分析试运行和分类结果清单。
4. 整理单 EXE 打包、版本、Release 资产和更新校验链路。

本阶段不以新增分类算法或大幅调整界面为主要目标。

## 二、业务场景

输电线路无人机巡检结束后，作业人员需要将大量带有 EXIF GPS 和拍摄时间的照片，依据线路经纬度台账归档到对应线路、杆塔和照片类别目录。照片数量多、文件命名和 EXIF 质量不完全稳定，人工分图耗时且容易遗漏。

当前工具已经能够完成主要分图工作，但真实生产使用还存在以下问题：

- 分类规则散落在多个模块中，手动与自主模式的部分判断不一致。
- 缺少自动化回归测试，调整规则后难以及时确认是否产生误分或漏分。
- 对台账缺列、异常塔号、坏图片、重复文件名等问题缺少统一预检结果。
- 日志以控制台文本为主，不便于逐张追溯“为什么被分类或被跳过”。
- 根目录源码、历史 Git 仓库、打包脚本和发布资产之间尚未形成唯一发布链路。

由于台账含杆塔坐标、照片含 GPS 和拍摄时间，项目必须继续坚持本地处理，并避免使用真实生产资料作为开发测试数据。

## 三、使用角色

### 3.1 一线作业人员

- 通过 Windows GUI 选择台账库、照片源目录、输出目录、线路和分类模式。
- 关注操作是否简单、分类进度、错误提示和最终输出是否完整。
- 需要快速找出未分类照片及其原因。

### 3.2 业务复核人员

- 抽查杆塔、精细化、红外和通道照片是否归档正确。
- 查看分类结果清单，定位无 GPS、超阈值、侧别不符或命名不规范的照片。
- 对低置信度或异常结果进行人工复核。

### 3.3 开发维护人员

- 修改分类规则、修复缺陷、维护 GUI 和扩展工具。
- 通过自动化测试确认修改没有破坏手动与自主分类链路。
- 从唯一权威源码构建、验证并发布单 EXE。

### 3.4 发布管理员

- 创建版本标签和 GitHub Release。
- 上传符合命名约定且通过完整性校验的 EXE。
- 验证升级、备份、失败回滚和重启流程。

> 一线作业、业务复核和发布管理员是否由同一批人员承担：需确认。

## 四、当前流程

### 4.1 GUI 操作流程

1. 启动 `gui_classifier.py` 或打包后的 EXE。
2. 选择台账库文件夹。
3. 添加一个或多个源照片文件夹。
4. 选择输出根目录，输入线路名称和距离阈值。
5. 选择手动飞行或自主飞行。
6. 点击“开始分类”，在日志控件中查看执行结果。
7. 分类完成后，通过输出目录人工抽查结果。

### 4.2 手动飞行流程

1. 读取 `{线路名称}经纬度台账.xlsx`。
2. 读取照片 EXIF GPS。
3. 计算照片与台账杆塔的距离，匹配最近杆塔。
4. 应用距离阈值和可选双回路侧别过滤。
5. 文件名包含 `_T` 时归入“红外照片”，否则归入“精细化”。
6. 为精细化照片对应杆塔创建“通道”空目录。

### 4.3 自主飞行流程

1. `classify_channels.py` 根据 `_V_` 照片的拍摄时间建立杆塔时间窗。
2. 提取通道照片并生成中间文件 `skip_ir.txt`。
3. `classify_autonomous.py` 读取 `skip_ir.txt`，分类剩余精细化和红外照片。
4. 所有源目录处理成功后，由 GUI 删除 `skip_ir.txt`。

### 4.4 当前验证与发布流程

- 主要依赖语法检查、侧别解析断言和人工 GUI 烟雾测试。
- 当前没有标准测试目录和依赖清单。
- 构建脚本位于历史仓库副本中，并在该目录执行 PyInstaller。
- 根目录 `.git` 当前不是完整 Git 仓库，无法作为正常工作树查看状态和回滚。

## 五、目标流程

### 5.1 目标用户流程

1. 用户填写分类参数。
2. 系统执行输入预检，展示错误、警告和统计摘要。
3. 用户可选择：
   - 仅分析：生成预检和预计分类结果，不复制照片。
   - 正式分类：确认后复制照片并生成结果清单。
4. 正式分类期间展示进度、当前源目录和分类统计。
5. 分类结束后生成结构化结果清单，并显示成功、跳过和失败数量。
6. 用户可根据清单定位异常照片，进行人工检查或修正源数据后重新运行。

### 5.2 目标开发流程

1. 所有修改只发生在根目录权威源码。
2. 每项分类规则修改先补充或更新自动化测试。
3. 完成修改后执行语法、单元、集成和脱敏样例回归。
4. 构建脚本从根目录权威源码生成单 EXE。
5. 发布前核对版本、文件名、Release tag、SHA-256 和更新回滚流程。

### 5.3 本阶段功能需求

| 编号 | 优先级 | 功能 | 需求说明 |
|---|---|---|---|
| FR-01 | P0 | 源码基线 | 根目录成为唯一可正常使用的 Git 工作树；历史副本只读参考，不参与日常构建。 |
| FR-02 | P0 | 依赖基线 | 提供明确的运行依赖、开发依赖和经过验证的 Python 版本说明。 |
| FR-03 | P0 | 自动化回归测试 | 覆盖距离、最近塔、命名、侧别、时间窗、跳过红外和输出目录等核心规则。 |
| FR-04 | P0 | 脱敏测试夹具 | 提供最小化台账和照片元数据夹具，不包含真实线路、人员或坐标。 |
| FR-05 | P0 | 输入预检 | 分类前检查路径、台账列、坐标、塔号、EXIF、命名、输出冲突等问题。 |
| FR-06 | P0 | 分类结果清单 | 每个扫描到的输入文件必须有一条最终结果记录。 |
| FR-07 | P1 | 仅分析模式 | 不复制照片，只输出预计分类结果、跳过原因和异常统计。 |
| FR-08 | P1 | 分类统计 | GUI 显示总数、已处理、已分类、跳过、失败及各原因数量。 |
| FR-09 | P0 | 权威构建 | 打包脚本必须从根目录源码构建，且不自动修改历史副本。 |
| FR-10 | P0 | 发布一致性 | `APP_VERSION`、Git tag、EXE 文件名、Release 资产名和更新器匹配规则保持一致。 |
| FR-11 | P0 | 更新完整性 | 发布更新必须支持强制 SHA-256 校验，失败时不得覆盖当前可用版本。 |
| FR-12 | P1 | 失败可诊断 | 单个坏文件应记录失败原因并继续处理，非致命异常不得中断整个批次。 |

### 5.4 成功指标

- 核心分类规则具备自动化回归用例，手动与自主两条链路均有覆盖。
- 使用固定脱敏样例重复运行时，输出文件集合和结果清单保持一致。
- 每张被扫描的文件都有明确状态，不再只能从自由文本日志判断结果。
- 仅分析模式不会向分类目录复制照片，也不会修改源目录。
- 打包产物可追溯到明确版本和提交，并能通过最小烟雾测试。
- 离线或 GitHub 更新检查失败时，不影响主分类功能。

## 六、输入内容

### 6.1 用户输入

- 台账库文件夹。
- 一个或多个源照片文件夹。
- 输出根目录。
- 线路名称。
- 距离阈值，单位为米。
- 分类模式：手动飞行或自主飞行。
- 执行方式：仅分析或正式分类。
- 冲突处理策略：每次运行开始时选择一次，默认“覆盖”（D-02 已确认）。
- 结果清单脱敏开关：默认完整记录，可勾选脱敏输出（D-04 已确认）。

### 6.2 台账输入

线路经纬度台账至少包含：

- `杆塔编号`
- `经度`
- `纬度`

自主通道提取当前还要求：

- `线路名称`

双回塔台账当前使用：

- `杆塔1名称`
- `杆塔2名称`
- `杆塔1方位`
- `杆塔2方位`

### 6.3 照片输入

- 文件路径和文件名。
- EXIF GPS 纬度、经度和方向标记。
- 自主模式所需 EXIF 拍摄时间。
- 文件名中的 `_V_`、`_T_` 或手动模式 `_T` 标识。
- 支持格式（D-07 已确认）：`.jpg/.jpeg/.tif/.tiff/.png`；其余扩展名记录“格式不支持”，不尝试解析 EXIF。

### 6.4 系统输入

- 当前软件版本。
- 本机保存的台账库路径配置。
- 发布更新元数据、Release 资产地址和 SHA-256。
- 当前输出目录已有文件状态。

## 七、输出内容

### 7.1 分类目录

正式分类继续保持现有结构：

```text
<output_root>/<line_name>/
├── 精细化/<tower>/
├── 红外照片/<tower>/
├── 通道/<tower>/
└── skip_ir.txt              # 仅允许在自主两阶段之间临时存在
```

### 7.2 分类结果清单

每次运行生成独立文件，建议命名：

```text
分图结果清单_YYYYMMDD_HHMMSS.csv
```

默认采用 Excel 可直接打开的 UTF-8 BOM CSV（D-03 已确认，本期不输出 `.xlsx`）。GUI 提供“脱敏输出”开关（D-04 已确认）：脱敏时坐标保留两位小数、源文件路径只保留文件名。

清单至少包含：

| 字段 | 说明 |
|---|---|
| 运行编号 | 标识同一次批处理 |
| 源目录 | 照片所属输入目录 |
| 文件名 | 原始文件名 |
| 源文件完整路径 | 用于定位原文件；默认完整，脱敏模式只保留文件名 |
| EXIF 状态 | 正常、无 GPS、无时间、解析失败等 |
| 纬度/经度 | 成功读取时记录；默认完整，脱敏模式保留两位小数 |
| 最近杆塔 | 最近匹配结果 |
| 最近距离 | 单位为米 |
| 期望侧别 | 台账配置的左、右或无过滤 |
| 实际侧别 | 计算结果 |
| 分类结果 | 精细化、红外照片、通道、跳过或失败 |
| 结果原因 | 超阈值、侧别不符、命名不识别等 |
| 目标路径 | 正式分类时记录实际目标；仅分析时记录预计目标 |
| 冲突处理 | 无冲突、覆盖、跳过或重命名 |
| 处理时间 | 该条记录生成时间 |

### 7.3 运行摘要

GUI 和日志至少输出：

- 扫描文件总数。
- 成功分类数量及分类分布。
- 跳过数量及原因分布。
- 解析失败数量。
- 输出冲突数量。
- 结果清单位置。
- 总耗时。

### 7.4 构建与发布输出

- 单 EXE 构建产物。
- SHA-256 文件或发布元数据。
- 对应版本说明。
- 构建和烟雾测试结果记录。

## 八、核心业务规则

### BR-01 权威源码

- 根目录是唯一权威源码。
- `repo_tmp/gui_classifier/` 只用于查阅历史 Git、README 和构建资料。
- 日常开发、测试和构建不得从历史副本反向覆盖根目录。

### BR-02 源文件保护

- 分类过程只读取源照片和台账。
- 不得移动、删除或改写源照片。
- 测试必须使用脱敏副本和独立临时输出目录。

### BR-03 分类兼容性

- 本阶段默认不改变现有手动、自主分类业务结果。
- 如测试证明现有规则互相冲突，应先记录差异并单独确认，再修改业务规则。
- 手动和自主模式的命名、侧别或台账查找规则统一属于后续独立任务，不能在基础设施改造中静默改变。

### BR-04 自主模式顺序

- 必须先运行通道提取，再运行精细化和红外分类。
- `skip_ir.txt` 是两个阶段之间的接口，第二阶段读取前不得删除。
- 全部源目录成功处理后必须删除 `skip_ir.txt`。
- 执行失败时应明确记录中间文件状态，不得误报“总体分类完成”。

### BR-05 一文件一结果

- 每个被扫描的输入文件必须在结果清单中恰好出现一次最终状态。
- 状态至少区分：已分类、无 GPS、无拍摄时间、超阈值、侧别不符、命名不识别、格式不支持、解析失败、输出冲突和系统失败。
- 一张照片经过多个内部阶段时，只输出一个最终业务结果，可额外记录处理阶段。

### BR-06 仅分析模式

- 可读取源文件、台账和输出目录状态。
- 可生成结果清单和日志。
- 不得向“精细化”“红外照片”“通道”目录复制照片。
- 不得残留 `skip_ir.txt` 或其他分类中间文件。
- 允许在输出根目录写入结果清单和运行日志（D-05 已确认）。

### BR-07 输出冲突

- 本阶段必须检测目标路径已存在的情况并记录到清单。
- 每次运行开始弹窗选择一次冲突策略，默认“覆盖”以保持现有行为（D-02 已确认）。
- 结果清单逐文件记录“无冲突/覆盖/跳过/自动重命名”；自动重命名列入后续迭代。

### BR-08 预检严重级别

- 错误：无法继续，例如台账不存在、必需列缺失、输出目录不可用。
- 警告：允许用户确认后继续，例如部分照片无 GPS、存在未知命名、目标文件冲突。
- 信息：不影响执行，例如某个可选双回塔台账不存在。
- 多个源目录必须分别统计，同时提供本次任务汇总。

### BR-09 更新与回滚

- 更新包来源必须匹配固定 GitHub 仓库和资产命名规则。
- 覆盖旧 EXE 前必须完成完整性校验并保留可恢复备份。
- 下载、校验、复制或重启任一步骤失败时，应保留当前可用版本并给出明确提示。

### BR-10 数据敏感性

- 结果清单中的坐标、源路径和拍摄时间按敏感生产资料处理。
- 不得将真实台账路径、真实坐标或照片信息写入仓库测试夹具、README 或构建日志。
- 本阶段不引入云端上传或外部 AI 服务。

## 九、异常情况

### 9.1 数据为空

- 台账无记录：阻止分类并提示。
- 线路名称过滤后无杆塔：阻止对应模式执行。
- 源目录为空：记录警告，不创建虚假的分类成功结果。
- 没有任何有效照片：生成摘要并说明原因。

### 9.2 必需字段缺失或格式错误

- 台账缺少必需列：预检错误，列出缺失字段。
- 经度、纬度为空或无法转换：标记具体台账行，不参与距离匹配。
- 杆塔编号无法排序或转换为整数：预检时明确报告；本阶段不宣称支持复合塔号。
- 距离阈值不是正数：GUI 阻止提交。

### 9.3 文件与 EXIF 异常

- 无 GPS：跳过分类并写入清单。
- 自主模式无拍摄时间：不能参与通道时间窗，但保持现状继续参与精细化/红外分类（D-06 已确认）；清单记录“无拍摄时间”警告。
- 损坏图片或非图片文件：单文件失败，不中断整个批次。
- 扩展名大小写或格式不支持：记录为格式不支持，不尝试复制为正常结果。

### 9.4 下游步骤无结果

- 通道提取没有找到任何通道照片：允许继续第二阶段，但必须在摘要中提示。
- `skip_ir.txt` 不存在或为空：第二阶段按空跳过列表处理并记录状态。
- 某杆塔没有对应照片：不视为系统错误。

### 9.5 用户修改与系统结果不一致

- 用户手工移动或删除输出照片后再次运行时，系统按当前输出状态重新检查冲突。
- 本阶段不自动回写用户的人工调整，也不修改原始台账。
- 后续可设计人工复核结果和系统建议之间的差异记录。

### 9.6 多任务或多数据源混合

- 多个源目录按添加顺序处理，但结果清单必须保留源目录字段。
- 不同线路不得在同一次任务中混用；多线路批处理列入后续迭代。
- 自主模式的中间状态不得错误复用到另一线路或另一运行任务。

### 9.7 重复点击或并发任务

- 任务运行期间禁用“开始分类”按钮。
- 同一 GUI 实例不得同时运行两个分类任务。
- 重复点击不得重复复制照片或并发重定向全局输出。

### 9.8 外部接口和发布失败

- GitHub API 超时、限流或离线：更新检查失败不得影响主程序启动和分类。
- Release 缺少匹配资产：不执行更新，提示发布配置错误。
- SHA-256 不一致：立即取消更新并删除或隔离下载文件。
- 新 EXE 启动失败：保留旧版本备份并提供恢复方式。

## 十、边界说明

### 10.1 本次做什么

- 修复根目录开发与版本管理基线。
- 建立依赖说明和经过验证的 Python 环境。
- 建立脱敏测试夹具及核心分类回归测试。
- 实现分类前输入预检。
- 实现每文件一条记录的结构化结果清单。
- 实现仅分析模式。
- 调整构建脚本，使其从根目录权威源码构建。
- 统一版本、EXE、Release 资产和更新校验约定。

### 10.2 本次不做什么

- 不改变现有输出目录层级。
- 不增加云端服务、数据库或账号体系。
- 不引入 AI 图像识别。
- 不支持非数字或复合杆塔编号，除非另立需求并补充规则。
- 不重写整个 Tkinter GUI。
- 不一次性合并或重写三个分类模块。
- 不自动修改真实台账、照片或用户人工整理结果。
- 不处理 `gui_classifier_updated.py`、`gui_classifier_pencil.py` 的归档决策。

### 10.3 后续迭代方向

1. 统一 EXIF、距离、最近塔、侧别和命名识别的共享分类核心。
2. 增加线程安全日志、进度条、取消任务和断点续跑。
3. 增加输出冲突策略和批量人工复核。
4. 增加地图预览、照片位置、杆塔连线和低置信度提示。
5. 支持人工改塔号、改类别和修正记录追踪。
6. 评估复合塔号、多线路批处理及更多相机命名规范。

### 10.4 人工确认和回退边界

- 每次运行开始时确认一次冲突策略（默认覆盖，D-02 已确认）；未完成策略选择时不允许开始正式分类。
- 自动更新覆盖当前 EXE 前必须完成完整性校验和旧版备份。
- 分类规则差异由回归测试暴露后，需业务确认预期结果再修改。
- 若新版本分类结果与已确认基线不一致，应停止发布并回退到上一可用版本。

## 十一、涉及模块初判

| 模块 | 预计影响 | 风险等级 |
|---|---|---|
| `gui_classifier.py` | 增加预检、仅分析入口、统计展示和任务状态控制 | 中高 |
| `classify.py` | 接入结构化结果记录，保持手动规则不变 | 中高 |
| `classify_channels.py` | 记录通道阶段决策和中间状态 | 高 |
| `classify_autonomous.py` | 读取阶段结果并产生唯一最终状态 | 高 |
| `side_parser.py` | 增加单元测试，原则上不改变现有逻辑 | 低 |
| `multi_mode_launcher.py` | 验证源码和冻结 EXE 子模式，不优先改动 | 中 |
| `button1.py`、`button2.py`、`button3.py` | 参与打包回归，首期不扩展功能 | 低至中 |
| `updater_github.py` | 增加强制校验、失败回退和资产一致性 | 高 |
| 构建脚本与 spec | 迁移为根目录权威构建入口 | 高 |
| 新增测试目录 | 单元测试、集成测试、脱敏夹具和预期结果 | 中 |
| 依赖与版本文档 | 固定运行/开发依赖和验证环境 | 中 |

本阶段跨越分类、GUI、测试和发布链路，建议拆分为多个可独立验收的任务，不建议一次提交全部修改。

## 十二、关键决策（已确认）

以下决策于 2026-08-15 全部按推荐方案确认，完整记录见 `DECISIONS.md`：

| 编号 | 决策项 | 结论 |
|---|---|---|
| D-01 | 目标发布版本 | 1.1.0 |
| D-02 | 同名文件冲突策略 | 运行开始弹窗选择一次，默认覆盖，清单记录冲突 |
| D-03 | 结果清单格式 | UTF-8 BOM CSV，本期不输出 xlsx |
| D-04 | 清单敏感字段 | 默认完整记录，提供脱敏输出开关 |
| D-05 | 仅分析模式输出 | 允许写报告，不创建分类目录和 skip_ir.txt |
| D-06 | 无拍摄时间照片 | 保持现状，仍参与精细化/红外分类，清单记警告 |
| D-07 | 支持格式 | jpg/jpeg/tif/tiff/png，其余记“格式不支持” |
| D-08 | 发布资产名 | 统一为“御3T分图工具.exe”，同步更新器 |
| D-09 | 打包环境 | 固定 Python 3.12 |
| D-10 | Git 基线 | 历史仓库 .git 迁移到根目录，根目录成为唯一仓库 |

## 十三、下一步建议

### 13.1 建议拆分顺序

#### 任务 A：源码、依赖和构建基线

- 明确根目录 Git 恢复方案。
- 建立依赖清单和验证环境说明。
- 将构建入口调整到根目录。
- 验证主 GUI 和四个 `--tool` 模式。

验收点：能够从根目录明确版本的源码稳定生成单 EXE，且不会打包历史 GUI。

#### 任务 B：测试基线

- 建立测试目录和脱敏夹具。
- 覆盖侧别、距离、命名、时间窗及 `skip_ir.txt` 生命周期。
- 固化当前已确认行为，不在本任务中改变业务规则。

验收点：固定样例重复运行输出一致，规则变更导致差异时测试能够失败。

#### 任务 C：结构化分类结果

- 设计统一结果记录模型。
- 让手动、自主两个阶段写入一致的状态和原因。
- 输出 CSV 和运行摘要。

验收点：每个扫描文件恰好有一条最终结果，人工能够根据清单解释分类或跳过原因。

#### 任务 D：预检和仅分析模式

- 对路径、台账、EXIF、命名和输出冲突执行预检。
- 在 GUI 增加仅分析入口和预检结果展示。
- 确保仅分析不复制照片、不残留中间文件。

验收点：典型错误在正式复制前被发现，试运行不会改变分类目录和源目录。

#### 任务 E：发布与更新闭环

- 统一版本和资产命名。
- 强制 SHA-256 校验。
- 验证离线、下载失败、校验失败、替换失败和回滚。

验收点：失败更新不会破坏当前版本，成功更新后版本与 Release 一致。

### 13.2 本阶段总体验收标准

- 所有根目录 Python 文件通过静态语法检查。
- 自动化测试覆盖手动和自主两条分类链路的关键规则。
- 测试数据不包含真实线路、真实坐标、人员信息或生产路径。
- 分类过程不修改源照片。
- 正式分类的目录结构与当前约定兼容。
- 自主模式成功完成后不存在 `skip_ir.txt`。
- 每张扫描文件在结果清单中有且仅有一条最终状态。
- 仅分析模式不复制照片，不残留分类中间文件。
- 单个坏文件不会导致整个批次丢失结果清单。
- 从根目录构建的 EXE 能启动主 GUI 和所有扩展工具模式。
- 自动更新失败不影响现有版本继续使用。
- 版本、tag、EXE 名称、Release 资产和更新匹配规则一致。

### 13.3 开发约束

后续任务实施时，应遵循以下约束：

> 只修改根目录权威源码，不反向覆盖历史副本；先建立或补充回归测试，再修改分类行为；不得使用真实巡检台账和照片试跑；不得移动或删除源照片；手动和自主链路必须分别验收；自主模式必须保持“通道提取 → 精细化/红外分类 → 成功后清理 skip_ir.txt”的顺序；涉及输出覆盖、分类规则差异、版本命名和 Git 迁移方案时，先取得明确确认；完成后报告修改文件、测试证据、已知风险和回滚方式。

### 13.4 推荐的紧接步骤

1. 第十二节关键决策已全部确认（见 `DECISIONS.md`）。
2. 按 13.1 顺序将本 PRD 拆成可独立开发与验收的任务卡。
3. 优先执行“任务 A：源码、依赖和构建基线”。
4. 在任何分类规则重构前完成“任务 B：测试基线”。
