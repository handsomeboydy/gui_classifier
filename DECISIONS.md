# 关键决策记录（已确认）

> 状态：已确认（2026-08-15）  
> 关联文档：`PRD.md` 第十二节  
> 规则：每项决策给出推荐方案与备选；本文件状态与 PRD 第十二节保持同步。

## 决策总览

| 编号 | 决策项 | 推荐方案 | 状态 |
|---|---|---|---|
| D-01 | 目标发布版本 | 1.1.0 | 已确认 |
| D-02 | 同名文件冲突策略 | 运行开始弹窗选择一次，默认“覆盖”以兼容现状，并记录冲突 | 已确认 |
| D-03 | 结果清单格式 | UTF-8 BOM CSV | 已确认 |
| D-04 | 清单敏感字段 | 默认完整记录，提供“脱敏输出”开关 | 已确认 |
| D-05 | 仅分析模式输出 | 允许在输出根目录写清单与日志，不创建分类目录和 skip_ir.txt | 已确认 |
| D-06 | 自主模式无拍摄时间照片 | 保持现状：仍参与精细化/红外分类，清单记录警告 | 已确认 |
| D-07 | 支持的照片格式 | 显式支持 jpg/jpeg，兼容 tif/tiff、png；其余记录“格式不支持” | 已确认 |
| D-08 | 发布资产名 | 统一为 `御3T分图工具.exe`，同步更新器匹配规则 | 已确认 |
| D-09 | 打包 Python 版本 | 固定 Python 3.12 为正式构建环境 | 已确认 |
| D-10 | Git 基线方案 | 将历史仓库 `.git` 迁移到根目录，根目录成为唯一仓库 | 已确认 |

---

## D-01 目标发布版本

- 背景：本期新增预检、仅分析、结构化结果清单等用户可见功能，并包含工程与测试基线。
- 推荐：`1.1.0`。属于功能增量，按语义版本号提升次版本。
- 备选：`1.0.2`（若把本期整体视为维护加固）。
- 影响：同步修改 `APP_VERSION`、Git tag、Release 资产名和更新器版本比较。

## D-02 同名文件冲突策略

- 背景：现有 `shutil.copy` 对相同目标路径静默覆盖；直接改为“跳过”可能让重复运行的用户误以为文件丢失。
- 推荐：每次运行开始弹窗选择一次冲突策略，默认“覆盖”以保持现有行为；预检阶段检测冲突数量，结果清单逐文件记录“无冲突/覆盖/跳过/自动重命名”。
- 备选：默认跳过；或每次遇到冲突都弹窗询问（批量场景容易打断操作）。
- 影响：本期至少实现冲突检测、清单记录和一次性的策略选择；自动重命名可放到后续迭代。

## D-03 结果清单格式

- 背景：清单需要被 Excel 打开、方便人工抽查，也应便于程序比较。
- 推荐：UTF-8 BOM CSV，字段按 PRD 第七章定义。
- 备选：同时输出 `.xlsx`（openpyxl 已是运行依赖，成本低）。
- 影响：本期只输出 CSV；如需 xlsx，后续增加导出开关。

## D-04 清单敏感字段

- 背景：清单用于本地人工复核，需要完整路径定位文件；坐标与路径属于敏感生产资料，仓库和分享场景不得包含真实数据。
- 推荐：默认包含完整源路径与 GPS；GUI 增加“脱敏输出”勾选项，勾选后坐标仅保留两位小数、路径只保留文件名。
- 影响：清单生成需要脱敏分支；仓库测试夹具始终使用虚构坐标。

## D-05 仅分析模式输出

- 背景：仅分析需要产出一份可查看的报告，但又不能污染正式分类结果。
- 推荐：允许在 `<output_root>/<line_name>/` 写入清单与运行日志；不得创建“精细化/红外照片/通道”目录，不得写入 `skip_ir.txt`。
- 备选：输出到独立“分析报告”目录，完全不写输出根目录。
- 影响：选择推荐方案则用户无需多选目录；PRD BR-06 明确禁止残留中间文件。

## D-06 自主模式无拍摄时间照片

- 背景：`classify_autonomous` 不依赖拍摄时间，无时间但有 GPS 的 `_V_/_T_` 照片现在仍会参与精细化/红外分类；只有通道时间窗环节需要时间。
- 推荐：保持现状不改变业务结果；清单中为这些照片记录“无拍摄时间”警告。
- 备选：无拍摄时间一律跳过（更保守，但改变现有结果）。
- 影响：推荐方案不产生回归差异。

## D-07 支持的照片格式

- 背景：现在 `glob('*.*')` 会尝试解析目录里所有文件，损坏图片或非图片文件可能触发异常。
- 推荐：显式支持 `.jpg/.jpeg/.tif/.tiff/.png`；不在清单内的文件在结果清单记录“格式不支持”，不尝试解析 EXIF。
- 备选：继续允许任意扩展名参与解析。
- 影响：这是行为收紧，非图片不再进入 EXIF 解析；需用御 3T 实际输出格式做一次回归确认。

## D-08 发布资产名

- 背景：构建脚本输出 `御3T分图工具.exe`，更新器却只匹配 `mavic3T.exe`，两者不一致会导致“检查更新”找不到资产。
- 推荐：统一为 `御3T分图工具.exe`，并同步修改 `updater_github.py` 的 `ASSET_NAME_PATTERN`；GitHub Release 上传同名资产。
- 备选：统一为 `mavic3T.exe`，由构建脚本把产物改名。
- 影响：任选其一都要同时改构建与更新器；推荐与 GUI 标题和构建名保持一致的中文名。

## D-09 打包 Python 版本

- 背景：本机当前解释器为 3.14.3 且未安装 `exifread`；项目声明 Python 3.8+，但没有锁定正式构建环境。
- 推荐：正式构建环境固定 Python 3.12，依赖按该版本锁定；源码保持 3.8+ 语法兼容。
- 备选：直接以当前 3.14 环境为准，先完成全链路验证再固定。
- 影响：需要在实际打包机确认或安装 3.12 环境，并建立依赖清单。

## D-10 Git 基线方案

- 背景：根目录 `.git` 是空壳（无 HEAD/config/objects）；`repo_tmp/gui_classifier/.git` 才是真实仓库，远程为 `handsomeboydy/gui_classifier`，分支 `main`。
- 推荐：将 `repo_tmp/gui_classifier/.git` 复制（保留原目录作为备份）到根目录，使历史完整保留且根目录成为唯一工作树；随后审查 `git status` 差异，把构建产物、录屏和本地配置按策略忽略；`repo_tmp` 保留为纯参考目录。
- 备选 A：根目录重新 `git init`（丢失现有历史，不推荐）。
- 备选 B：暂不处理 Git，继续直接开发（无版本基线，后续无法安全回滚，不推荐）。
- 影响：这是工程性动作，涉及 `.git` 目录和文件归属校验；执行前需要用户确认，并先做备份。

---

## 确认记录

- 确认日期：2026-08-15
- 确认结果：D-01 至 D-10 全部按推荐方案确认。
- 已同步：`PRD.md` 第十二节改为“关键决策（已确认）”并更新相关业务规则。
- 待执行：影响行为的决策（D-02、D-04、D-06、D-07、D-08、D-10）写入对应开发任务卡后实施。
