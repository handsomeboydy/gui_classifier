# Python 版本约定

## 正式打包环境（目标）

- 版本：Python 3.12（决策 D-09，2026-08-15 确认）。
- 理由：pandas 与 PyInstaller 生态在 3.12 上兼容性成熟，适合作为正式发布构建环境。
- 发布前必须使用该版本完成一次完整构建和 EXE 冒烟验证。

## 当前验证环境（差异记录）

- 本机当前可用解释器：Python 3.14.3（默认）、Python 3.13。
- 本机未安装 Python 3.12，当前任务 A 的验证与构建使用 3.14.3 完成。
- 已知差异：使用不同 Python 版本构建的 PyInstaller 单文件 EXE 版本不同；正式发布前需在 3.12 环境重建并回归。

## 环境准备建议

1. 安装 Python 3.12（建议 3.12.x 最新补丁版）。
2. 创建专用虚拟环境：

   ```powershell
   py -3.12 -m venv .venv-312
   .\.venv-312\Scripts\activate
   pip install -r requirements-dev.txt
   ```

3. 构建与验证均在该环境中执行。

## 源码兼容性

- 源码保持 Python 3.8+ 语法兼容（项目原有声明）。
- 新代码不得使用仅 3.9+/3.10+ 可用的语法，除非同时更新版本声明。
