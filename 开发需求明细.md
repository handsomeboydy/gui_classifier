# 开发需求明细：分图工具 1.1.0 工程化升级

> 用途：作为设置开发目标、拆分任务卡和验收的完整清单。  
> 来源：`PRD.md`、`DECISIONS.md`（2026-08-15 已确认）。  
> 版本目标：1.1.0；当前基线：1.0.1。  
> 约束：只修改根目录权威源码；不反向覆盖 `repo_tmp/gui_classifier/`；不使用真实台账、照片和路径开发试跑。

## 一、总目标

在保持现有手动飞行、自主飞行、Windows GUI、单 EXE 多模式启动和自动升级能力不被破坏的前提下，完成四类工程化升级：

1. 建立可追踪、可回滚的源码与构建基线。
2. 为核心分类规则建立脱敏、可重复执行的回归测试。
3. 增加分类前预检、仅分析试运行和分类结果清单。
4. 整理单 EXE 打包、版本、Release 资产和更新校验链路。

## 二、已确认关键决策（开发约束）

| 编号 | 决策 | 结论 |
|---|---|---|
| D-01 | 目标发布版本 | 1.1.0 |
| D-02 | 同名文件冲突 | 每次运行开始弹窗选择一次策略，默认“覆盖”，清单记录冲突 |
| D-03 | 结果清单格式 | UTF-8 BOM CSV，本期不输出 xlsx |
| D-04 | 清单敏感字段 | 默认完整记录路径与 GPS；提供“脱敏输出”开关（坐标两位小数、路径只留文件名） |
| D-05 | 仅分析模式输出 | 允许在输出根目录写清单和日志；不创建分类目录、不写 skip_ir.txt |
| D-06 | 自主模式无拍摄时间照片 | 保持现状：不参与通道时间窗，仍参与精细化/红外分类，清单记“无拍摄时间” |
| D-07 | 支持的照片格式 | 显式支持 jpg/jpeg/tif/tiff/png；其余记录“格式不支持”，不解析 EXIF |
| D-08 | 发布资产名 | 统一为“御3T分图工具.exe”，同步更新器匹配规则 |
| D-09 | 打包环境 | 固定 Python 3.12 为正式构建环境，源码保持 3.8+ 兼容 |
| D-10 | Git 基线 | 历史仓库 .git 迁移到根目录，根目录成为唯一仓库 |

## 三、功能需求清单（FR-01 至 FR-12）

| 编号 | 优先级 | 功能 | 需求明细 |
|---|---|---|---|
| FR-01 | P0 | 源码基线 | 根目录成为唯一可正常使用的 Git 工作树；历史副本只读参考，不参与日常构建。 |
| FR-02 | P0 | 依赖基线 | 提供运行依赖、开发依赖和经过验证的 Python 版本（3.12）说明。 |
| FR-03 | P0 | 自动化回归测试 | 覆盖距离、最近塔、命名、侧别、时间窗、跳过红外和输出目录等核心规则。 |
| FR-04 | P0 | 脱敏测试夹具 | 最小化台账和照片元数据夹具，不包含真实线路、人员或坐标。 |
| FR-05 | P0 | 输入预检 | 分类前检查路径、台账列、坐标、塔号、EXIF、命名、输出冲突等问题。 |
| FR-06 | P0 | 分类结果清单 | 每个扫描到的输入文件必须有一条最终结果记录。 |
| FR-07 | P1 | 仅分析模式 | 不复制照片，只输出预计分类结果、跳过原因和异常统计。 |
| FR-08 | P1 | 分类统计 | GUI 显示总数、已处理、已分类、跳过、失败及各原因数量。 |
| FR-09 | P0 | 权威构建 | 打包脚本必须从根目录源码构建，且不自动修改历史副本。 |
| FR-10 | P0 | 发布一致性 | APP_VERSION、Git tag、EXE 文件名、Release 资产名和更新器匹配规则一致。 |
| FR-11 | P0 | 更新完整性 | 更新必须支持强制 SHA-256 校验，失败时不得覆盖当前可用版本。 |
| FR-12 | P1 | 失败可诊断 | 单个坏文件记录失败原因并继续处理，非致命异常不中断整个批次。 |

## 四、任务拆分与详细需求

### 任务 A：源码、依赖和构建基线

详细任务卡：`task_cards/TASK_A_源码依赖构建基线.md`

工作项：

- A1 将 `repo_tmp/gui_classifier/.git` 复制到根目录，原目录保留为备份；处理 `safe.directory` 归属问题。
- A2 建立 `.gitignore`：忽略 build/、dist/、__pycache__/、*.pyc、gui_classifier_config.json、button3_config.json、使用录屏.mp4、repo_tmp/。
- A3 审查 git status 与历史提交，建立根目录基线提交。
- A4 新增 requirements.txt（pandas、exifread、openpyxl、pillow）与 requirements-dev.txt（含 pyinstaller），安装后锁定版本。
- A5 新增 PYTHON_VERSION.md，固定正式打包环境为 Python 3.12。
- A6 将 build_exe.bat 与 spec 迁移到根目录，入口使用根目录 gui_classifier.py，产物统一为“御3T分图工具.exe”。
- A7 将 gui_classifier.py 中两次 dispatch_mode_or_continue() 去重为一次，保持在创建 Tk 主窗口之前。
- A8 将 updater_github.py 的 ASSET_NAME_PATTERN 改为匹配“御3T分图工具.exe”（SHA-256 等其余改动属任务 E）。
- A9 验证：语法检查、side_parser 断言、四个 --tool 模式、主 GUI 启动、构建产物名与更新器匹配。

验收点：从根目录明确版本的源码可稳定生成单 EXE，不会打包历史 GUI；版本、产物名、更新器匹配一致。

### 任务 B：测试基线

工作项：

- B1 建立测试目录（建议 pytest；如不希望引入测试依赖，可用标准库 unittest，验收等价）。
- B2 建立脱敏夹具：虚构线路经纬度台账 xlsx、双回塔台账 xlsx、带 GPS/时间的合成照片；坐标与线路名称全部虚构。
- B3 覆盖 side_parser：整线左/右、分段规则、全角标点、单点、非法输入。
- B4 覆盖手动分类：最近塔匹配、阈值内外、阈值边界、_T 红外判定、精细化空通道目录、侧别过滤。
- B5 覆盖自主通道：_V_ 时间窗、时间窗边界、无时间照片、skip_ir.txt 生成内容。
- B6 覆盖自主第二阶段：读取 skip_ir、跳过对应红外、_V_/_T_ 分类、侧别过滤。
- B7 覆盖 skip_ir.txt 生命周期：两阶段之间必须存在，成功完成后必须删除。
- B8 覆盖异常：无 GPS、坏图片、非数字塔号、空台账、缺列、重复文件名、输出冲突。
- B9 固定预期输出文件集合，验证重复运行一致性。

验收点：固定脱敏样例重复运行输出一致；规则变更导致差异时测试能够失败。

### 任务 C：结构化分类结果

工作项：

- C1 定义统一结果记录模型，字段按 PRD 7.2：运行编号、源目录、文件名、源路径、EXIF 状态、经纬度、最近塔、距离、期望/实际侧别、分类结果、结果原因、目标路径、冲突处理、处理时间。
- C2 让 classify.py、classify_channels.py、classify_autonomous.py 输出统一结构记录，不改变分类结果。
- C3 一文件一结果：每个扫描文件恰好一条最终状态，区分已分类、无 GPS、无拍摄时间、超阈值、侧别不符、命名不识别、格式不支持、解析失败、输出冲突、系统失败。
- C4 输出 UTF-8 BOM CSV：`<输出根目录>/<线路名称>/分图结果清单_YYYYMMDD_HHMMSS.csv`。
- C5 实现“脱敏输出”开关（D-04）：脱敏时坐标两位小数、路径只留文件名。
- C6 生成运行摘要：总数、分类分布、跳过原因分布、解析失败、冲突数、清单位置、总耗时。
- C7 GUI 展示分类统计（FR-08）。

验收点：每张扫描文件恰好一条最终结果，人工可依据清单解释分类或跳过原因。

### 任务 D：预检和仅分析模式

工作项：

- D1 实现分类前预检：台账存在与必需列、坐标可转换、塔号可排序、线路过滤非空、源目录非空、输出目录可用、双回塔台账存在、照片格式支持、EXIF 抽样、命名统计、输出冲突检测。
- D2 预检分级：错误（阻止执行）、警告（确认后继续）、信息（不影响执行）。
- D3 实现仅分析模式：可读取源文件、台账和输出状态，生成清单与日志；不创建“精细化/红外照片/通道”目录、不复制照片、不写 skip_ir.txt（D-05）。
- D4 实现冲突策略选择：每次运行开始弹窗选择一次，默认覆盖（D-02）；未选择时不允许开始正式分类。
- D5 GUI 增加仅分析入口、预检结果展示和冲突策略选择。
- D6 验证重复点击与并发任务：运行期间禁用“开始分类”按钮，同一实例不得并发两个任务。

验收点：典型错误在正式复制前被发现；试运行不改变分类目录和源目录。

### 任务 E：发布与更新闭环

工作项：

- E1 统一版本链路：APP_VERSION 1.1.0、Git tag、构建产物名、Release 资产名和更新器匹配规则一致。
- E2 强制 SHA-256 校验：prepare_update 必须校验发布资产摘要，不一致则取消更新并隔离下载文件。
- E3 更新失败保护：下载、校验、复制或重启任一步骤失败时保留当前可用版本并提示；覆盖前保留 .bak 备份。
- E4 更新缓存与限频：保留 6 小时检查间隔，离线/超时/限流不影响主程序启动和分类。
- E5 构建产物校验与最小烟雾测试：构建后验证主 GUI 与四个 --tool 模式可启动。
- E6 建立发布记录：版本说明、SHA-256、构建时间和资产名。

验收点：失败更新不会破坏当前版本；成功更新后版本与 Release 一致。

## 五、全局验收标准

- [ ] 所有根目录 Python 文件通过静态语法检查。
- [ ] 自动化测试覆盖手动和自主两条分类链路的关键规则。
- [ ] 测试数据不包含真实线路、真实坐标、人员信息或生产路径。
- [ ] 分类过程不修改源照片。
- [ ] 正式分类的目录结构与当前约定兼容。
- [ ] 自主模式成功完成后不存在 skip_ir.txt。
- [ ] 每张扫描文件在结果清单中有且仅有一条最终状态。
- [ ] 仅分析模式不复制照片，不残留分类中间文件。
- [ ] 单个坏文件不会导致整个批次丢失结果清单。
- [ ] 从根目录构建的 EXE 能启动主 GUI 和所有扩展工具模式。
- [ ] 自动更新失败不影响现有版本继续使用。
- [ ] 版本、tag、EXE 名称、Release 资产和更新匹配规则一致。

## 六、明确不做（非目标）

- 不改变现有输出目录层级。
- 不增加云端服务、数据库或账号体系。
- 不引入 AI 图像识别。
- 不支持非数字或复合杆塔编号（除非另立需求）。
- 不重写整个 Tkinter GUI。
- 不一次性合并或重写三个分类模块（规则统一列入后续迭代）。
- 不自动修改真实台账、照片或人工整理结果。
- 不处理 gui_classifier_updated.py、gui_classifier_pencil.py 的归档决策。
- 不实现地图预览、人工复核、置信度、断点续跑、多线路批处理（后续迭代）。

## 七、开发约束

> 只修改根目录权威源码，不反向覆盖历史副本；先建立或补充回归测试，再修改分类行为；不得使用真实巡检台账和照片试跑；不得移动或删除源照片；手动和自主链路必须分别验收；自主模式必须保持“通道提取 → 精细化/红外分类 → 成功后清理 skip_ir.txt”的顺序；涉及输出覆盖、分类规则差异、版本命名和 Git 迁移方案时，先取得明确确认；完成后报告修改文件、测试证据、已知风险和回滚方式。

## 八、建议执行顺序

1. 任务 A：源码、依赖和构建基线。
2. 任务 B：测试基线（任何分类规则重构前必须完成）。
3. 任务 C：结构化分类结果。
4. 任务 D：预检和仅分析模式。
5. 任务 E：发布与更新闭环。

每项任务独立开发、独立验收；完成一项后再进入下一项。
